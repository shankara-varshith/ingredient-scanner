const { GoogleGenerativeAI } = require("@google/generative-ai");
require("dotenv").config({ path: ".env.local" });

async function run() {
  // Let's use curl directly to list models to see if API key is correct
}
run();
